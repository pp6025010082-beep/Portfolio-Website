# Base URL of the backend API.
# Local development (using the Vite proxy in vite.config.js): leave as /api
# Production: set to your deployed backend URL, e.g. https://api.yourdomain.com/api
VITE_API_URL=/api
